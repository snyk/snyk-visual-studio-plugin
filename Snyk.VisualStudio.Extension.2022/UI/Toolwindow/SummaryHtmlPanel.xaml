<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             x:Class="Snyk.VisualStudio.Extension.UI.Toolwindow.SummaryHtmlPanel"
             toolkit:Themes.UseVsTheme="True"
             UseLayoutRounding="True"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <!-- RootGrid is painted the themed tool-window background in code so the area reads correctly
         while the WebView2 is collapsed (until its first navigation completes), hiding WebView2's
         dark pre-initialization surface. -->
    <Grid x:Name="RootGrid">
        <wv2:WebView2 x:Name="SummaryHtmlViewer"
                      Visibility="Collapsed"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch" />
    </Grid>
</UserControl>