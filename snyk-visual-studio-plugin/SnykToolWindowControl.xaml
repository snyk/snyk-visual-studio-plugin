<UserControl x:Class="snyk_visual_studio_plugin.SnykToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow">
    <Grid>
        <StackPanel Orientation="Vertical">
            <DataGrid x:Name="resultsDataGrid" Height="300" UseLayoutRounding="False" Padding="1" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
            <TextBlock Margin="10" HorizontalAlignment="Center"><Run Text="SnykToolWindow"/></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
